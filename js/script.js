const nav=document.querySelector('.nav'),menu=document.querySelector('.menu');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in')),{threshold:.08});document.querySelectorAll('.section,.hero-left,.hero-right,.project,.skill,.journey-grid article').forEach(e=>{e.classList.add('reveal');obs.observe(e)});
document.querySelectorAll('.filter button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');let f=btn.dataset.filter;document.querySelectorAll('.skill').forEach(s=>s.style.display=f==='all'||s.dataset.cat===f?'grid':'none')}));
function clock(){document.querySelector('#clock').textContent=new Date().toLocaleTimeString('pt-BR',{hour12:false})}clock();setInterval(clock,1000);
const dot=document.querySelector('.cursor-dot'),cur=document.querySelector('.cursor');window.addEventListener('mousemove',e=>{cur.style.transform=`translate(${e.clientX-12}px,${e.clientY-12}px)`;dot.style.transform=`translate(${e.clientX-2}px,${e.clientY-2}px)`});document.querySelectorAll('a,button').forEach(e=>{e.addEventListener('mouseenter',()=>cur.classList.add('hover'));e.addEventListener('mouseleave',()=>cur.classList.remove('hover'))});
